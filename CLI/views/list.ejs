<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>LIST</title>
</head>
<body>
    <div class="container mt-2">
        <h1>Gallery</h1>
        <% data.forEach(dat => { %>
            <div class="card">
                <div class="card-header">
                    <h3 class="card-title"><%= dat.key %></h3>
                </div>
                <div class="card-body">
                    <img src="<%= dat.data %>" width="200px" class="card-img-top" alt="<%= dat.Key %>">
                </div>
            </div>
        <% }); %>
    </div>
</body>
</html>